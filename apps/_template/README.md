# Mini app template

Copy this folder:

```bash
cp -R apps/_template apps/your-app-name
```

Then:

1. Edit `apps/your-app-name/index.html`.
2. Add the app to `/apps.json`.
3. Push to `main`.

The app will be served from `apps/your-app-name/`.
